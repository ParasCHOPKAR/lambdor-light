import Hero from "./components/Hero"; 

export default function Home() {
  return (
    <>
      {/* This renders the interactive light-switch Hero at the top of the homepage */}
      <Hero />
      
      {/* As you build more sections (like About, Featured Products, etc.), 
          you will add them right here below the Hero! */}
    </>
  );
}